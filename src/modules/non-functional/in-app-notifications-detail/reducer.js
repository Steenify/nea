// import update from 'react-addons-update';

const initialState = {
  ui: {},
  data: {},
};

const reducer = (state = initialState, action) => {
  const { type } = action;

  switch (type) {
    default:
      return state;
  }
};

export default reducer;
