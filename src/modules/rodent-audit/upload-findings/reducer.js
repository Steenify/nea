// import update from 'react-addons-update';

const initialState = {
  ui: { isLoading: false },
  data: {},
};

export default (state = initialState, { type }) => {
  switch (type) {
    default:
      return state;
  }
};
